<Window x:Class="Städteliste.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Städteliste"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center">
        <Button x:Name="btn_back" VerticalAlignment="Center" HorizontalAlignment="Left" Width="100" Height="100" Margin="0,0,10,0" Command="{Binding CmdbuttonClick}" CommandParameter="back" >zurück</Button>
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label>Name:</Label>
                <TextBox x:Name="txt_name" Width="300" Text="{Binding Namen, Mode=OneWay}"></TextBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label>Land:</Label>
                <TextBox x:Name="txt_country" Width="300" Text="{Binding Country, Mode=OneWay}" ></TextBox>
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label>Einwohnerzahl:</Label>
                <TextBox x:Name="txt_population" Width="300" Text="{Binding Population, Mode=OneWay}"></TextBox>
            </StackPanel>
            <CheckBox x:Name="check_isCapital" IsChecked="{Binding isCapital, Mode=OneWay}">Hauptstadt</CheckBox>
        </StackPanel>
        <Button x:Name="btn_forward" VerticalAlignment="Center" HorizontalAlignment="Right" Width="100" Height="100" Margin="10,0,0,0" Command="{Binding CmdbuttonClick}" CommandParameter="forward">weiter</Button>
    </StackPanel>
</Window>
